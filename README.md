# Тестовое задание "Крестики-нолики 5-в-ряд"

Установка зависимостей: `npm i`

Для разработки: `npm start` (сервер запускается на `localhost:3000`)

Готовая сборка: `npm run build` (результат в папке build)
